{
  "name": "swarm",
  "version": "1.2.0",
  "description": "SWARM — browser horde-survival roguelite",
  "type": "module",
  "scripts": {
    "dev": "python3 -m http.server 5174",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0"
  },
  "engines": { "node": ">=20" }
}
